# CRM Photo Export

## Установка
```bash
pip install -r requirements.txt

Внутри папки проекта:

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

python export.py --from 2026-03-30 --to 2026-04-13